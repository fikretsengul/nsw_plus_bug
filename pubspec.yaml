name: nsw_plus_bug
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  nested_scroll_view_plus: ^1.0.2

dev_dependencies:
  custom_lint: ^0.5.8
  pyramid_lint: ^1.3.0

flutter:
  uses-material-design: true
